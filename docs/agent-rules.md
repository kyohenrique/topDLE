# Agent Rules Applied

- Keep code simple, readable, and componentized.
- Avoid unnecessary dependencies.
- Prefer typed static data for predictable behavior.
- Use small reusable helpers for filtering and translations.
- Respect App Router rendering split:
  - server for URL/bootstrap
  - client for interaction
- Preserve performance by avoiding over-fetching and large runtime abstractions.
